{
  "name": "mmmri-client",
  "description": "Visualize results of brain scans.",
  "version": "0.0.1",
  "devDependencies": {
    "grunt": "~0.4.1",
    "grunt-contrib-uglify": "~0.2.2",
    "grunt-contrib-jshint": "~0.6.2",
    "grunt-contrib-concat": "~0.3.0",
    "grunt-contrib-watch": "~1.1.0",
    "grunt-ngmin": "0.0.3",
    "grunt-contrib-less": "~0.7.0"
  },
  "scripts": {
    "build": "grunt",
    "postinstall": "npx bower install && grunt",
    "unzip:brainbrowser": "cd bower_components/brainbrowser/examples/color-maps; ls;  gunzip -d *.gz ; ls; cd ../models; gunzip -d *.gz ; ls;"
  }
}
